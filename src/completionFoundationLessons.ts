import { makeCatalog, type CatalogSpec } from './completionFactory'

const array='#78d8ff', linear='#62e4d0'
const specs: CatalogSpec[]=[
  {id:'prefix-sum-2d',categoryId:'search-sort',subcategory:'陣列技巧',category:'ARRAY',title:'2D Prefix Sum',zhTitle:'二維前綴和',description:'以容斥把矩形總和化成四次查表。',complexity:'Build O(HW) · Query O(1)',visual:'array',accent:array,code:['for(int r=0;r<h;++r) for(int c=0;c<w;++c)','  pref[r+1][c+1]=a[r][c]+pref[r][c+1]+pref[r+1][c]-pref[r][c];','long long rectangle(int r1,int c1,int r2,int c2){','  return pref[r2+1][c2+1]-pref[r1][c2+1]-pref[r2+1][c1]+pref[r1][c1];','}'],concepts:['pref[r][c] 定義為半開矩形 [0,r)×[0,c)；建表時上方與左方相加會重複左上角，因此減回一次。','查詢矩形先取右下大前綴，再扣掉上方與左方；兩者共同扣除的左上角需加回。','半開定義讓貼邊矩形不需特判，建表 O(HW)，每次查詢只做四次存取。'],states:['half-open prefix rectangle','four-term inclusion-exclusion','constant-time rectangle sum']},
  {id:'prefix-xor',categoryId:'search-sort',subcategory:'陣列技巧',category:'ARRAY',title:'Prefix XOR',zhTitle:'前綴異或',description:'利用 x XOR x=0 消去區間外前綴。',complexity:'Build O(n) · Query O(1)',visual:'array',accent:array,code:['vector<int> px(n+1,0);','for(int i=0;i<n;++i) px[i+1]=px[i]^a[i];','int rangeXor(int l,int r){','  return px[r+1]^px[l];','}'],concepts:['px[i] 是 a[0..i) 的 XOR；XOR 具結合律，且每個值與自己 XOR 為 0。','px[r+1] 與 px[l] 的共同前綴出現兩次而消去，只留下 a[l..r]。','與前綴和不同，XOR 沒有進位且是自身逆元素，因此同樣可 O(1) 查詢。'],states:['px[i]=xor [0,i)','cancel common prefix','range XOR answer']},
  {id:'counting-sort',categoryId:'search-sort',subcategory:'排序與選擇',category:'SORT',title:'Counting Sort',zhTitle:'計數排序',description:'值域小時直接計數並依序輸出。',complexity:'O(n+K)',visual:'array',accent:array,code:['vector<int> count(K+1);','for(int x:a) ++count[x];','int p=0; for(int value=0;value<=K;++value)','  while(count[value]--) a[p++]=value;'],concepts:['count[v] 精確保存值 v 的出現次數；這一步不比較元素，只把輸入投影到值域桶。','由小到大掃值域，每個 v 輸出 count[v] 次，因此輸出天然非遞減。','成本依賴值域 K；若要穩定排序紀錄，先把 count 轉成結束位置並由右向左放置。'],states:['frequency buckets','emit values in key order','sorted multiset restored']},
  {id:'inversion-counting',categoryId:'search-sort',subcategory:'排序與選擇',category:'SORT',title:'Inversion Counting',zhTitle:'逆序對計數',description:'在 Merge Sort 合併時計算跨半部逆序。',complexity:'O(n log n)',visual:'array',accent:array,code:['long long solve(int l,int r){ if(r-l<=1) return 0; int m=(l+r)/2;','  long long ans=solve(l,m)+solve(m,r);','  for(merge pointers i,j) if(a[i]<=a[j]) takeLeft();','  else ans+=m-i, takeRight();','  copyMergedBack(); return ans; }'],concepts:['逆序對分為完全在左半、完全在右半與跨越兩半；遞迴先計算前兩類。','合併兩個有序半部時，若右值小於左指標值，它也小於左側所有剩餘 m−i 個元素，立即加上這批逆序。','每層合併 O(n)、共 O(log n) 層，且每個逆序恰在其兩端第一次分居兩半時被計一次。'],states:['split inversion classes','right value beats all remaining left','merge and accumulate']},
  {id:'meet-in-the-middle',categoryId:'search-sort',subcategory:'陣列技巧',category:'SEARCH',title:'Meet in the Middle',zhTitle:'折半枚舉',description:'把指數枚舉拆成兩半並配對結果。',complexity:'O(2^(n/2) log 2^(n/2))',visual:'array',accent:array,code:[
    'vector<long long> buildSubsetSums(const vector<long long>& values) {',
    '  vector<long long> sums = {0};',
    '  for (long long value : values) {',
    '    int oldSize = (int)sums.size();',
    '    for (int i = 0; i < oldSize; ++i) {',
    '      sums.push_back(sums[i] + value);',
    '    }',
    '  }',
    '  return sums;',
    '}',
    '',
    'long long maxSubsetSumAtMost(const vector<long long>& a, long long limit) {',
    '  int middle = (int)a.size() / 2;',
    '  vector<long long> firstHalf(a.begin(), a.begin() + middle);',
    '  vector<long long> secondHalf(a.begin() + middle, a.end());',
    '  vector<long long> leftSums = buildSubsetSums(firstHalf);',
    '  vector<long long> rightSums = buildSubsetSums(secondHalf);',
    '',
    '  sort(rightSums.begin(), rightSums.end());',
    '  long long answer = LLONG_MIN;',
    '',
    '  for (long long left : leftSums) {',
    '    long long remaining = limit - left;',
    '    auto it = upper_bound(rightSums.begin(), rightSums.end(), remaining);',
    '    if (it == rightSums.begin()) continue;',
    '    long long right = *prev(it);',
    '    answer = max(answer, left + right);',
    '  }',
    '  return answer;',
    '}',
  ],concepts:['任一完整子集合唯一拆成左半選擇與右半選擇，因此答案是兩個半部和 x+y 的最佳合法配對。','各枚舉 2^(n/2) 個子集合和；排序右側後，對每個 x 二分最大 y≤limit−x。','把 2^n 降為約 2^(n/2) 的時間與空間，適合 n 約 40 的 subset 類問題。'],states:['enumerate both halves','sort and binary-search complements','combine unique full subset']},
  {id:'parallel-binary-search',categoryId:'search-sort',subcategory:'離線查詢',category:'SEARCH',title:'Parallel Binary Search',zhTitle:'整體二分',description:'讓多個查詢共享同一輪事件掃描。',complexity:'O((E+Q) log A · updateCost)',visual:'array',accent:array,code:['while(existsUnresolvedQuery()){','  bucketQueriesByMid(); resetDataStructure();','  for(int t=0;t<events;++t){ apply(event[t]);','    for(int q:bucket[t]) if(predicate(q)) hi[q]=t; else lo[q]=t+1;','  }','}'],concepts:['每個查詢的 predicate 隨答案時間單調；各自維持 [lo,hi]，但同一輪把相同 mid 的查詢分桶。','事件依時間只掃一次，資料結構逐步加入前綴狀態；抵達 mid 時同時判定該桶全部查詢。','每輪所有區間折半，O(log A) 輪後收斂；必須能重建或回滾每輪資料結構。'],states:['one monotone interval per query','bucket by midpoint + one event sweep','all answer intervals converge']},
  {id:'interval-scheduling',categoryId:'search-sort',subcategory:'區間與貪心',category:'INTERVAL',title:'Interval Scheduling',zhTitle:'最多不重疊區間',description:'依結束時間選取最早完成的下一段。',complexity:'O(n log n)',visual:'array',accent:array,code:['sort(intervals.begin(),intervals.end(),byEnd);','int lastEnd=-INF,answer=0;','for(auto [l,r]:intervals) if(l>=lastEnd){','  ++answer; lastEnd=r;','}'],concepts:['在所有可選區間中，最早結束者為後續保留最大空間；選它不會比任何最優解的第一段更晚。','把任一最優解第一段交換成最早結束區間，後續仍全部可行且數量不減，形成交換論證。','排序後單次掃描，接受起點不早於 lastEnd 的區間；端點是否可相接依題目改成 > 或 ≥。'],states:['sort by finishing time','accept compatible earliest finish','maximum compatible set']},
  {id:'interval-covering',categoryId:'search-sort',subcategory:'區間與貪心',category:'INTERVAL',title:'Interval Covering',zhTitle:'最少區間覆蓋',description:'從目前端點選能延伸最遠的可用區間。',complexity:'O(n log n)',visual:'array',accent:array,code:['sort(intervals.begin(),intervals.end());','double covered=L; int i=0,answer=0;','while(covered<R){ double far=covered;','  while(i<n && intervals[i].l<=covered) far=max(far,intervals[i++].r);','  if(far==covered) return IMPOSSIBLE; covered=far; ++answer;','}'],concepts:['當前已覆蓋到 covered；下一段必須從 l≤covered 的候選中選，否則會留下缺口。','在所有可接候選中選右端 far 最遠者。任何解的下一段右端不會超過 far，因此交換後不增加段數。','若 far 沒前進則無法覆蓋；否則反覆延伸至 R，得到最少區間數。'],states:['covered prefix endpoint','choose farthest reachable right end','reach target or detect gap']},
  {id:'interval-merging',categoryId:'search-sort',subcategory:'區間與貪心',category:'INTERVAL',title:'Interval Merging',zhTitle:'區間合併',description:'排序後把重疊區間收進目前連通段。',complexity:'O(n log n)',visual:'array',accent:array,code:['sort(intervals.begin(),intervals.end());','for(auto [l,r]:intervals){','  if(out.empty() || l>out.back().second) out.push_back({l,r});','  else out.back().second=max(out.back().second,r);','}'],concepts:['依左端排序後，尚未處理區間不可能延伸到目前 merged 段左側；只需比較最後一段。','若 l≤currentRight，兩段相交或相接，右端更新為 max；否則中間有空隙，開始新段。','輸出互不重疊且聯集與輸入相同；每個區間只處理一次，排序主導成本。'],states:['sort by left endpoint','extend last union component','disjoint canonical intervals']},
  {id:'job-scheduling',categoryId:'search-sort',subcategory:'區間與貪心',category:'GREEDY',title:'Job Scheduling by Deadline',zhTitle:'截止時間排程',description:'按截止時間掃描並保留可行的最高價值工作。',complexity:'O(n log n)',visual:'array',accent:array,code:['sort(jobs.begin(),jobs.end(),byDeadline);','priority_queue<int> durations; long long used=0;','for(auto [deadline,duration]:jobs){ used+=duration; durations.push(duration);','  if(used>deadline){ used-=durations.top(); durations.pop(); }','}'],concepts:['依 deadline 遞增時，若已選集合總時間不超過目前 deadline，則所有更早 deadline 也可行。','加入新工作後若超時，刪除選中工作裡 duration 最大者，能留下同樣件數下最小總時間，對未來最有利。','交換論證保證每個前綴保留的件數最大；heap 支援刪除最大工時。'],states:['deadlines in order','drop longest when infeasible','maximum feasible job count']},
  {id:'huffman-coding',categoryId:'search-sort',subcategory:'區間與貪心',category:'GREEDY',title:'Huffman Coding',zhTitle:'霍夫曼編碼',description:'反覆合併兩個最低頻率節點建立最優前綴碼。',complexity:'O(n log n)',visual:'array',accent:array,code:['priority_queue<ll,vector<ll>,greater<ll>> pq(freq.begin(),freq.end());','long long cost=0; while(pq.size()>1){','  ll a=pq.top();pq.pop(); ll b=pq.top();pq.pop();','  cost+=a+b; pq.push(a+b);','}'],concepts:['最優前綴碼樹中，兩個最低頻率符號可安排為最深且互為兄弟；交換到此位置不會增加加權路徑長。','合併兩者成頻率 a+b 的虛擬符號，問題縮成少一個符號的同型子問題。','min-heap 每次取兩小；所有合併權重總和等於加權編碼長度，得到最優前綴碼。'],states:['two minimum frequencies','merge as sibling subtree','optimal prefix-code tree']},
  {id:'fractional-knapsack',categoryId:'search-sort',subcategory:'區間與貪心',category:'GREEDY',title:'Fractional Knapsack',zhTitle:'可分割背包',description:'依單位重量價值由高到低裝入。',complexity:'O(n log n)',visual:'array',accent:array,code:['sort(items.begin(),items.end(),[](auto a,auto b){return a.value*b.weight>b.value*a.weight;});','double answer=0;','for(auto item:items){ double take=min(capacity,item.weight);','  answer+=take*item.value/item.weight; capacity-=take;','}'],concepts:['物品可任意切割，因此每單位容量彼此可交換；價值密度 value/weight 是每單位容量的收益。','若解中取了較低密度單位卻未取更高密度單位，交換相同重量會增加價值，與最優矛盾。','依密度排序後依序全取，最後一件可部分取；不可分割 0/1 背包不具此交換性。'],states:['sort by value density','fill capacity greedily','possibly take final fraction']},
  {id:'largest-rectangle-histogram',categoryId:'linear-structures',subcategory:'Stack',category:'STACK',title:'Largest Rectangle in Histogram',zhTitle:'柱狀圖最大矩形',description:'單調 Stack 在右界確定時結算高度。',complexity:'O(n)',visual:'linear',accent:linear,code:['stack<int> st;','for(int i=0;i<=n;++i){ int h=i==n?0:height[i];','  while(!st.empty() && height[st.top()]>h){ int k=st.top();st.pop();','    int left=st.empty()?0:st.top()+1; answer=max(answer,height[k]*(i-left)); }','  st.push(i);','}'],concepts:['stack 中索引高度非遞減；某柱 k 留在 stack 時，尚未看到它右側第一個更矮柱。','遇到高度 h 更小的 i，k 的最大延伸右界確定為 i−1；pop 後新 top 決定左側第一個更矮柱。','加入尾端高度 0 哨兵結算全部柱；每個索引 push/pop 各一次，總成本 O(n)。'],states:['monotone increasing heights','pop fixes maximal width','maximum area settled']},
  {id:'sliding-window-maximum',categoryId:'linear-structures',subcategory:'Queue 與 Deque',category:'DEQUE',title:'Sliding Window Maximum',zhTitle:'滑動視窗最大值',description:'單調 Deque 保存未被支配的候選索引。',complexity:'O(n)',visual:'linear',accent:linear,code:['deque<int> dq;','for(int i=0;i<n;++i){','  while(!dq.empty() && dq.front()<=i-k) dq.pop_front();','  while(!dq.empty() && a[dq.back()]<=a[i]) dq.pop_back();','  dq.push_back(i); if(i>=k-1) answer.push_back(a[dq.front()]);','}'],concepts:['deque 索引遞增、對應值嚴格遞減；front 因此是目前視窗最大值。','先從 front 移除離開視窗者；再從 back 移除不大於新值者，因新值更晚過期且至少同大。','每個索引最多進出一次，front 始終是仍在視窗內的最大候選，因此所有視窗答案可在線性時間依序產生。'],states:['indices increasing, values decreasing','expire front / dominate back','front is each window maximum']},
  {id:'expression-evaluation',categoryId:'linear-structures',subcategory:'Stack',category:'STACK',title:'Expression Evaluation',zhTitle:'運算式求值',description:'以數值與運算子 Stack 實作優先序。',complexity:'O(n)',visual:'linear',accent:linear,code:['for(Token t:tokens){','  if(isNumber(t)) values.push(t.value);','  else if(t=="(") ops.push(t);','  else { while(shouldReduce(ops.top(),t)) applyTop(values,ops); ops.push(t); }','}','while(!ops.empty()) applyTop(values,ops);'],concepts:['values 保存已讀運算元與部分結果，ops 保存尚未套用的運算子；括號形成不可跨越的界線。','新運算子到來時，先結算優先序更高或同級左結合的 stack top，確保原運算順序。','每個 token push/pop 常數次；完整讀取並清空 ops 後，values top 是唯一結果。'],states:['operand and operator stacks','reduce by precedence / parentheses','single final value']},
  {id:'shunting-yard',categoryId:'linear-structures',subcategory:'Stack',category:'STACK',title:'Shunting Yard',zhTitle:'調度場演算法',description:'將中序運算式轉為後序序列。',complexity:'O(n)',visual:'linear',accent:linear,code:['for(Token t:tokens){','  if(isOperand(t)) output.push_back(t);','  else if(isOperator(t)){ while(higherPrecedenceOnStack(t)) popToOutput(); ops.push(t); }','  else if(t=="(") ops.push(t); else { while(ops.top()!="(") popToOutput(); ops.pop(); }','}','while(!ops.empty()) popToOutput();'],concepts:['後序輸出中運算元先於使用它的運算子；operator stack 延遲尚不能確定位置的符號。','遇到運算子時，把應先執行的 stack 運算子輸出；右括號則輸出直到對應左括號但不輸出括號。','輸出序列可用單一值 stack 線性求值，且完全保留原優先序與結合性。'],states:['operators wait on stack','pop by precedence and parentheses','valid postfix sequence']},
]

export const completionFoundationLessons=makeCatalog(specs)
