import { makeCatalog, type CatalogSpec } from './completionFactory'

const ds='#72e6b7', dp='#ffad72'
const specs: CatalogSpec[]=[
  {id:'iterative-segment-tree',categoryId:'data-structures',subcategory:'區間資料結構',category:'RANGE',title:'Iterative Segment Tree',zhTitle:'迭代式線段樹',description:'以底層葉陣列向上合併並用雙指標查詢。',complexity:'O(log n)',visual:'range',accent:ds,code:['for(int i=n-1;i>0;--i) tree[i]=tree[i<<1]+tree[i<<1|1];','long long query(int l,int r){ long long ans=0;','  for(l+=n,r+=n;l<r;l>>=1,r>>=1){','    if(l&1) ans+=tree[l++]; if(r&1) ans+=tree[--r];','  } return ans; }'],concepts:['葉節點存於 [n,2n)，父節點 i 合併 2i 與 2i+1；tree[1] 是全區間。','半開查詢 [l,r) 同步上移：l 是右孩子時取它再右移，r 是右邊界時先左移取其左兄弟。','被取節點互斥且聯集恰為原區間；兩端每層至多取兩節點，O(log n)。'],states:['flat complete binary tree','consume boundary nodes upward','disjoint cover merged']},
  {id:'dynamic-segment-tree',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'RANGE',title:'Dynamic Segment Tree',zhTitle:'動態開點線段樹',description:'只為實際修改到的巨大座標區間建立節點。',complexity:'O(log C) per operation',visual:'range',accent:ds,code:['void add(Node*& p,ll l,ll r,ll x,ll delta){','  if(!p) p=new Node(); if(r-l==1){p->sum+=delta;return;}','  ll m=l+(r-l)/2; if(x<m) add(p->left,l,m,x,delta); else add(p->right,m,r,x,delta);','  p->sum=sum(p->left)+sum(p->right);','}'],concepts:['根代表完整座標域，但 null 子樹等價於全零；只有更新路徑上的節點需要存在。','每層按中點選一側遞迴，抵達葉後沿路重算 sum；座標域大小 C 決定高度。','m 次點更新只建立 O(m log C) 節點，適合座標極大且稀疏、無法直接離散化的情況。'],states:['null subtree means zero','allocate one root-to-leaf path','sparse nodes only']},
  {id:'merge-sort-tree',categoryId:'data-structures',subcategory:'區間資料結構',category:'RANGE',title:'Merge Sort Tree',zhTitle:'合併排序樹',description:'每個節點保存排序後區間以回答值域統計。',complexity:'Build O(n log n) · Query O(log² n)',visual:'range',accent:ds,code:['build(node,l,r){ if(r-l==1) tree[node]={a[l]};','  else build(left),build(right),merge(children,tree[node]); }','int countLE(node,l,r,ql,qr,x){ if(disjoint) return 0;','  if(covered) return upper_bound(tree[node].begin(),tree[node].end(),x)-tree[node].begin();','  return countLE(left)+countLE(right); }'],concepts:['每個 array 元素出現在每一層的一個節點，節點向量是其區間全部值的排序結果。','位置區間分解成 O(log n) 節點；在每個排序向量二分 x，計算 ≤x 的個數。','共 O(log n) 個節點、每次二分 O(log n)；空間 O(n log n)，可再以 fractional cascading 改善。'],states:['sorted vector per interval','decompose positions + binary search values','aggregate counts']},
  {id:'segment-tree-beats',categoryId:'data-structures',subcategory:'區間資料結構',category:'RANGE',title:'Segment Tree Beats',zhTitle:'線段樹 Beats',description:'以最大值、次大值與次數剪枝區間 chmin。',complexity:'Amortized O(log² n)',visual:'range',accent:ds,code:['void chmin(int p,int l,int r,int ql,int qr,ll x){','  if(disjoint || max1[p]<=x) return;','  if(covered && max2[p]<x){ applyMax(p,x); return; }','  push(p); chmin(left,...,x); chmin(right,...,x); pull(p);','}'],concepts:['節點保存 max1、嚴格次大 max2、最大值出現次數與 sum；chmin(x) 只會改變大於 x 的值。','若 max2<x<max1，只有所有最大值降為 x，可整段更新 sum 與 max1；否則必須下推。','每次無法整段處理會使某節點的最大值結構實質下降，勢能分析給出攤銷對數界。'],states:['max1 / max2 / countMax','apply when max2 < x < max1','potential decreases on descent']},
  {id:'fenwick-tree-2d',categoryId:'data-structures',subcategory:'區間資料結構',category:'RANGE',title:'2D Fenwick Tree',zhTitle:'二維樹狀陣列',description:'在兩個維度分解低位元區塊。',complexity:'O(log H log W)',visual:'range',accent:ds,code:['void add(int x,int y,ll v){ for(int i=x;i<=h;i+=i&-i)','  for(int j=y;j<=w;j+=j&-j) bit[i][j]+=v; }','ll prefix(int x,int y){ ll s=0; for(int i=x;i;i-=i&-i)','  for(int j=y;j;j-=j&-j) s+=bit[i][j]; return s; }'],concepts:['bit[i][j] 保存兩維各自 lowbit 決定的尾端矩形；所有這些矩形能分解前綴 [1,x]×[1,y]。','查詢在 x、y 各減 lowbit；更新則各加 lowbit，把點貢獻傳給所有涵蓋它的區塊。','每維走 O(log size) 步，矩形查詢再以四個前綴容斥。'],states:['lowbit rectangle cells','nested lowbit walks','four-prefix rectangle query']},
  {id:'segment-tree-2d',categoryId:'data-structures',subcategory:'區間資料結構',category:'RANGE',title:'2D Segment Tree',zhTitle:'二維線段樹',description:'以 x 樹節點內嵌 y 樹維護矩形資訊。',complexity:'O(log² n) per operation',visual:'range',accent:ds,code:['updateX(vx,lx,rx,x,y,val){','  if(rx-lx==1) updateY(vx,y,val);','  else updateX(childContaining(x),...,x,y,val), rebuildY(vx);','}','queryRectangle(x1,x2,y1,y2)=queryX(root,x1,x2,y1,y2);'],concepts:['外層節點代表 x 區間，每個節點的內層樹維護該 x 區間投影到 y 的聚合。','點更新沿 O(log n) 個 x 節點，並在每個內層 y 樹更新 O(log n)；矩形查詢同理分解。','直接版本空間 O(n²)；稀疏或離線壓縮內層座標可降到 O(n log n)。'],states:['x-tree of y-trees','log nodes in each dimension','rectangle aggregates merged']},
  {id:'disjoint-sparse-table',categoryId:'data-structures',subcategory:'區間資料結構',category:'RANGE',title:'Disjoint Sparse Table',zhTitle:'不相交稀疏表',description:'為每層區塊預算中點向外的前後綴。',complexity:'Build O(n log n) · Query O(1)',visual:'range',accent:ds,code:['for(each level k and block){ build suffix to midpoint; build prefix from midpoint; }','T query(int l,int r){ if(l==r) return a[l];','  int k=msb(l^r);','  return suffix[k][l] op prefix[k][r];','}'],concepts:['在每個 2^(k+1) 區塊，以中點為界建立左側 suffix 與右側 prefix，運算只需具結合律。','對 l<r，k=msb(l XOR r) 是兩者第一次分居該層中點兩側；[l,r] 恰由一個 suffix 與 prefix 組成。','每次只合併兩值 O(1)，且不要求冪等，故可處理 sum、乘積等一般 monoid。'],states:['midpoint suffix/prefix per level','msb(l xor r) selects level','combine exactly two blocks']},
  {id:'wavelet-tree',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'RANGE',title:'Wavelet Tree',zhTitle:'小波樹',description:'依值域中點穩定分割並保留位置映射。',complexity:'O(log σ) per query',visual:'range',accent:ds,code:['build(values,lo,hi){ if(lo==hi)return; mid=(lo+hi)/2;','  pref[i+1]=pref[i]+(values[i]<=mid); stablePartition(values,mid);','  build(left,lo,mid); build(right,mid+1,hi);','}','// kth(l,r,k) maps [l,r) with pref counts'],concepts:['每節點序列依值≤mid 穩定送左，其餘送右；pref[i] 記前 i 個送左數量，可把任意位置區間映射到孩子。','kth 查詢先數 [l,r) 中左側元素 cnt；k≤cnt 進左，否則進右並令 k−=cnt。','每層值域減半，支援 kth、≤x 次數與頻率，空間 O(n log σ)。'],states:['stable split by value midpoint','prefix maps position interval','descend by rank count']},
  {id:'wavelet-matrix',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'RANGE',title:'Wavelet Matrix',zhTitle:'小波矩陣',description:'逐位元穩定分割，避免指標樹結構。',complexity:'O(bits) per query',visual:'range',accent:ds,code:['for(int bit=MAXB;bit>=0;--bit){','  rank0[bit]=prefixZeros(sequence,bit);','  stablePartitionZeroThenOne(sequence,bit); zeroCount[bit]=countZeros;','}','// map [l,r) using rank0 and zeroCount at each bit'],concepts:['從最高位到最低位，每層把 0-bit 元素穩定放前、1-bit 放後，記錄零數與 rank。','查詢沿目標值位元映射 l/r；選 1 時加上零區偏移，並可累計被跳過的較小元素。','平坦陣列比指標 wavelet tree 更快，固定 bits 層即可支援 rank、kth 與 range frequency。'],states:['stable bit partitions','rank maps interval each layer','bitwise order statistics']},
  {id:'implicit-treap',categoryId:'data-structures',subcategory:'平衡搜尋樹',category:'BST',title:'Implicit Treap',zhTitle:'隱式 Treap',description:'以子樹大小把序列位置當作鍵。',complexity:'Expected O(log n)',visual:'range',accent:ds,code:['pair<Node*,Node*> split(Node* t,int k){ if(!t)return {}; push(t);','  if(size(t->left)>=k){ auto [a,b]=split(t->left,k); t->left=b;pull(t);return {a,t}; }','  auto [a,b]=split(t->right,k-size(t->left)-1);t->right=a;pull(t);return {t,b}; }','Node* merge(Node*a,Node*b){ if(!a||!b)return a?a:b; return priority(a)>priority(b)?attachRight(a,b):attachLeft(a,b); }'],concepts:['中序順序就是序列順序；節點隱式索引等於左子樹大小，不需存實際 key。','split(t,k) 依 size 分成前 k 個與其餘；merge 依隨機 priority 維持 heap 性質且保持中序。','區間操作以兩次 split 取出中段，套用 lazy tag 後 merge 回去，期望高度 O(log n)。'],states:['inorder equals sequence','split by subtree size','range edit then merge']},
  {id:'splay-tree',categoryId:'data-structures',subcategory:'平衡搜尋樹',category:'BST',title:'Splay Tree',zhTitle:'伸展樹',description:'把剛存取節點旋轉到根以取得攤銷效率。',complexity:'Amortized O(log n)',visual:'range',accent:ds,code:['void splay(Node* x){ while(x->parent){ Node* p=x->parent,*g=p->parent;','  if(!g) rotate(x);','  else if((x==p->left)==(p==g->left)) rotate(p),rotate(x);','  else rotate(x),rotate(x);','} }'],concepts:['旋轉保持 BST 中序順序；splay 以 zig、zig-zig、zig-zag 把 x 移到根。','連續同向時先旋父再旋 x，可同時大幅降低 x 與祖先深度；反向時連旋 x。','單次可能 O(n)，但勢能分析使任意操作序列總成本 O(m log n)，且常用資料會靠近根。'],states:['BST order invariant','zig / zig-zig / zig-zag','accessed node becomes root']},
  {id:'link-cut-tree',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'DYNAMIC TREE',title:'Link-Cut Tree',zhTitle:'動態樹 Link-Cut',description:'以輔助 Splay 維護動態森林路徑。',complexity:'Amortized O(log n)',visual:'range',accent:ds,code:['void access(Node* x){ Node* last=nullptr;','  for(Node* y=x;y;y=y->pathParent){ splay(y); y->right=last; pull(y); last=y; }','  splay(x);','}','void makeRoot(Node*x){ access(x); x->reversed^=1; }'],concepts:['實樹被 preferred paths 分割，每條路徑用 splay 表示；輕邊以 path-parent 連接輔助樹。','access(x) 逐段切換 preferred child，使根到 x 的整條路徑落在同一 splay；makeRoot 再翻轉方向。','link、cut 與 path aggregate 都化成有限次 access/splay，攤銷 O(log n)。'],states:['preferred-path decomposition','access exposes root-to-x path','link / cut / query dynamically']},
  {id:'euler-tour-tree',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'DYNAMIC TREE',title:'Euler Tour Tree',zhTitle:'動態 Euler Tour Tree',description:'以平衡樹保存森林 Euler 序列支援 Link/Cut。',complexity:'O(log n)',visual:'range',accent:ds,code:['Sequence reroot(int v){ split tour before occurrence(v); return merge(suffix,prefix); }','void link(int u,int v){ auto A=reroot(u),B=reroot(v); tours=merge(A,edgeUV,B,edgeVU); }','void cut(edge e){ split around two directed occurrences of e); remove occurrences; }'],concepts:['樹的 Euler tour 把每條無向邊表示為兩次有向經過；同一棵樹對應平衡序列樹。','reroot 是序列旋轉；link 把兩棵 tour 以兩個方向邊串接，cut 在兩次出現處切開。','平衡 BST 的 split/merge O(log n)，並可在序列節點維護分量大小或頂點聚合。'],states:['tree as Euler sequence','link concatenates / cut splits','dynamic component aggregate']},
  {id:'cartesian-tree',categoryId:'data-structures',subcategory:'平衡搜尋樹',category:'TREE',title:'Cartesian Tree',zhTitle:'笛卡兒樹',description:'同時滿足索引中序與數值 Heap 性質。',complexity:'O(n)',visual:'range',accent:ds,code:['vector<int> st;','for(int i=0;i<n;++i){ int last=-1;','  while(!st.empty() && a[st.back()]>a[i]) last=st.back(),st.pop_back();','  if(!st.empty()) right[st.back()]=i; left[i]=last; st.push_back(i);','}'],concepts:['Cartesian tree 的中序是原索引順序，且父值≤子值；值互異時兩性質唯一決定整棵樹。','單調 stack 保存右脊；新值較小就彈出成其左子樹，剩餘 top 成新節點父親。','每點進出 stack 一次線性建樹；區間最小值等於兩端節點的 LCA。'],states:['inorder indices + min-heap values','monotone right spine','RMQ becomes LCA']},
  {id:'kd-tree',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'SPATIAL',title:'KD-Tree',zhTitle:'KD 樹',description:'交替座標軸分割空間並以包圍盒剪枝。',complexity:'Average O(log n), worst O(n)',visual:'range',accent:ds,code:['Node* build(points,depth){ int axis=depth%K,m=points.size()/2;','  nth_element(points.begin(),points.begin()+m,points.end(),byAxis(axis));','  return new Node(points[m],build(left,depth+1),build(right,depth+1));','}','nearest(node,q){ if(boxDistance(node)>=best)return; visitPoint(); visitNearThenFarChild(); }'],concepts:['每層以某座標軸中位數分割，節點子樹包圍盒包含其全部點；先後軸交替避免單一方向退化。','最近鄰查詢先走較近子盒；若某盒到 q 的理論最小距離已≥best，整個子樹不可能改善而剪掉。','平衡資料下平均快速，但高維剪枝變弱；距離比較通常用平方避免浮點開根。'],states:['median split axis','bounding-box lower bound','prune impossible subtree']},
  {id:'persistent-dsu',categoryId:'data-structures',subcategory:'持久化與動態結構',category:'PERSISTENCE',title:'Persistent DSU',zhTitle:'可持久化並查集',description:'以持久化陣列保存每次合併版本。',complexity:'O(log V) per operation',visual:'range',accent:ds,code:['Version unite(Version ver,int a,int b){ a=find(ver,a);b=find(ver,b);','  if(a==b)return ver; if(size(ver,a)<size(ver,b))swap(a,b);','  ver=parentArray.set(ver,b,a);','  ver=sizeArray.set(ver,a,size(ver,a)+size(ver,b)); return ver;','}'],concepts:['每個版本的 parent/size 是持久化 segment tree 根；單點修改只複製根到葉路徑，舊版本不變。','為控制修改量通常不做路徑壓縮，只按大小合併，使 find 深度 O(log V)。','每次 unite 產生新版本根，可在任意歷史版本查連通或分叉新時間線。'],states:['versioned parent arrays','path copying on union','query any historical version']},
  {id:'ordered-statistic-tree',categoryId:'data-structures',subcategory:'平衡搜尋樹',category:'BST',title:'Order Statistic Tree',zhTitle:'順序統計樹',description:'在平衡 BST 維護子樹大小以查排名。',complexity:'O(log n)',visual:'range',accent:ds,code:['Node* kth(Node* t,int k){ int left=size(t->left);','  if(k<left)return kth(t->left,k); if(k==left)return t;','  return kth(t->right,k-left-1);','}','int rank(Node*t,Key x)=size(keys<x);'],concepts:['每個節點維護 size=1+左右子樹大小，旋轉或合併後立即 pull，BST 順序與大小同時正確。','kth 比較 k 與 leftSize：進左、回傳目前、或扣除 left+1 進右。','rank(x) 累加所有確定小於 x 的左子樹與節點；平衡高度保證 O(log n)。'],states:['balanced BST + subtree sizes','branch by leftSize','rank and kth in logarithmic time']},
  {id:'fibonacci-dp',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'Fibonacci DP',zhTitle:'費波那契動態規劃',description:'以兩個已完成子問題建立下一狀態。',complexity:'O(n)',visual:'dp',accent:dp,code:['long long prev2=0,prev1=1;','for(int i=2;i<=n;++i){','  long long cur=prev1+prev2;','  prev2=prev1; prev1=cur;','} return n?prev1:prev2;'],concepts:['狀態 F[i] 只依賴 F[i−1]、F[i−2]，初值 F[0]=0、F[1]=1。','按 i 遞增時依賴都已完成；因只需前兩項，可用 prev2/prev1 滾動而不存整表。','每個狀態計算一次 O(n)，空間 O(1)；這也展示了 DP 的狀態、轉移、順序與初值。'],states:['base states F0,F1','F[i]=F[i-1]+F[i-2]','rolling state result']},
  {id:'unbounded-knapsack',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'Unbounded Knapsack',zhTitle:'完全背包',description:'同一物品可重複使用的容量 DP。',complexity:'O(nW)',visual:'dp',accent:dp,code:['vector<ll> dp(W+1,-INF); dp[0]=0;','for(auto [weight,value]:items)','  for(int cap=weight;cap<=W;++cap)','    dp[cap]=max(dp[cap],dp[cap-weight]+value);'],concepts:['dp[cap] 是處理到目前物品時容量 cap 的最大價值；由 dp[cap−weight] 加入同物品。','容量由小到大掃，使本輪剛更新的 dp[cap−weight] 可再次被使用，正好允許無限件。','若反向掃描則每件最多一次，變成 0/1 背包；迴圈方向是語意的一部分。'],states:['capacity value states','forward capacity reuses item','best value for every capacity']},
  {id:'subset-sum',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'Subset Sum',zhTitle:'子集合和',description:'維護使用前綴元素可達的所有總和。',complexity:'O(nS)',visual:'dp',accent:dp,code:['vector<char> can(S+1); can[0]=true;','for(int x:a)','  for(int sum=S;sum>=x;--sum)','    can[sum] = can[sum] || can[sum-x];'],concepts:['處理前 i 個元素後 can[s] 表示是否存在子集合和 s；空集合使 can[0]=true。','對新 x，狀態來自不選 x 的舊 can[s] 或選 x 的舊 can[s−x]。','sum 反向掃避免同一 x 在本輪重複使用；bitset 可把轉移寫成 bits |= bits<<x。'],states:['reachable sums after prefix','skip x or take x once','target reachability']},
  {id:'grid-dp',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'Grid DP',zhTitle:'網格動態規劃',description:'依拓樸方向累積到每個格子的最佳路徑。',complexity:'O(HW)',visual:'dp',accent:dp,code:['dp[0][0]=grid[0][0];','for(int r=0;r<h;++r) for(int c=0;c<w;++c){','  if(r) relax(dp[r][c],dp[r-1][c]+grid[r][c]);','  if(c) relax(dp[r][c],dp[r][c-1]+grid[r][c]);','}'],concepts:['若只允許向右/下，狀態圖是 DAG；dp[r][c] 是走到該格的最佳值。','所有進入 (r,c) 的最後一步只可能來自上或左，枚舉這兩個前驅即完整。','以 row-major 順序計算時依賴已完成；障礙格設為不可達並避免由不可達狀態轉移。'],states:['cell state on DAG','relax from top and left','destination optimum']},
  {id:'matrix-chain-multiplication',categoryId:'dynamic-programming',subcategory:'區間與樹 DP',category:'DP',title:'Matrix Chain Multiplication',zhTitle:'矩陣鏈乘法',description:'枚舉最後一次切分以最小化乘法次數。',complexity:'O(n³)',visual:'dp',accent:dp,code:['for(int len=2;len<=n;++len) for(int l=0;l+len<=n;++l){ int r=l+len;','  dp[l][r]=INF;','  for(int k=l+1;k<r;++k)','    dp[l][r]=min(dp[l][r],dp[l][k]+dp[k][r]+dim[l]*dim[k]*dim[r]);','}'],concepts:['dp[l][r] 是矩陣半開鏈 [l,r) 的最少 scalar multiplications；單一矩陣成本 0。','最優括號化的最後一步必在某 k 合併左右結果，成本為兩子問題加最終矩陣乘法。','依區間長度遞增確保子區間已知；枚舉 O(n²) 區間與 O(n) 切點。'],states:['interval multiplication state','enumerate final split k','minimum parenthesization cost']},
  {id:'interval-dp',categoryId:'dynamic-programming',subcategory:'區間與樹 DP',category:'DP',title:'Interval DP',zhTitle:'區間動態規劃',description:'依區間長度合併較小子區間。',complexity:'Typically O(n³)',visual:'dp',accent:dp,code:['for(int len=1;len<=n;++len)','  for(int l=0;l+len<=n;++l){ int r=l+len;','    initialize(dp[l][r]);','    for(int k=l+1;k<r;++k) relax(dp[l][r],combine(dp[l][k],dp[k][r]));','  }'],concepts:['狀態以連續區間 [l,r) 定義；任何合法拆分都產生嚴格更短的子區間。','外層按 len 遞增，枚舉所有 l 與最後切分 k，確保讀取 dp[l][k]、dp[k][r] 已完成。','正確性來自對最優解最後結構的完整枚舉；需明確定義空區間與單點基底。'],states:['short intervals first','enumerate last split / boundary choice','full interval answer']},
  {id:'dag-dp',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'DAG DP',zhTitle:'有向無環圖 DP',description:'依拓樸序把狀態沿邊傳遞。',complexity:'O(V+E)',visual:'dp',accent:dp,code:['vector<int> order=topologicalSort();','for(int u:order)','  for(int v:g[u])','    dp[v]=best(dp[v],transition(dp[u],u,v));'],concepts:['DAG 的拓樸序使每條邊 u→v 都由較早指向較晚，狀態依賴無循環。','處理 u 時其所有前驅貢獻已合併，因此可把最終 dp[u] 沿出邊轉移。','每點每邊一次，可求路徑數、最長路與多種資源狀態；起點與不可達初值必須明確。'],states:['topological dependency order','propagate final state over edges','all DAG states settled']},
  {id:'digit-dp',categoryId:'dynamic-programming',subcategory:'狀態壓縮',category:'DP',title:'Digit DP',zhTitle:'數位動態規劃',description:'逐位計數不超過上界且符合限制的數字。',complexity:'O(digits · states · base)',visual:'dp',accent:dp,code:['long long dfs(int pos,bool tight,bool started,int state){','  if(pos==digits.size()) return accept(state,started);','  if(!tight && memoKnown(pos,started,state)) return memo;','  long long ans=0; int hi=tight?digits[pos]:9;','  for(int d=0;d<=hi;++d) ans+=dfs(pos+1,tight&&d==hi,started||d,stateTransition(state,d));','  return storeIfLoose(ans); }'],concepts:['pos 是目前位，tight 表示前綴是否等於上界，started 區分前導零；額外 state 保存題目限制。','本位可選 0..hi；新 tight 必須同時舊 tight 且 d 等於上界該位，轉移後進下一位。','只有 tight=false 的子問題與實際上界前綴無關可記憶化；計算 [L,R] 用 count(R)−count(L−1)。'],states:['position / tight / started / constraint','enumerate legal next digit','memoize non-tight states']},
  {id:'profile-dp',categoryId:'dynamic-programming',subcategory:'狀態壓縮',category:'DP',title:'Profile DP',zhTitle:'輪廓線 DP',description:'用位元遮罩保存窄網格邊界狀態。',complexity:'O(HW·2^W·transitions)',visual:'dp',accent:dp,code:['dp[0][0]=1;','for(each cell pos) for(int mask=0;mask<(1<<W);++mask){','  if(mask&1) transferOccupied(pos,mask>>1);','  else { placeVertical(pos,mask); placeHorizontal(pos,mask); }','}'],concepts:['掃描網格時，已處理區與未處理區的互動只發生在寬度 W 的輪廓；mask 記錄每個邊界格是否已被佔用。','對目前格依最低位決定跳過或放置合法拼塊，更新下一位置的 mask；所有局部配置完整枚舉。','當 W 小時 2^W 可行；結尾只接受 mask=0，代表沒有突出邊界的未完成部分。'],states:['frontier bitmask','advance one cell with local placements','empty terminal profile']},
  {id:'probability-dp',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'Probability DP',zhTitle:'機率動態規劃',description:'以全機率公式累積狀態分布或期望。',complexity:'O(states · transitions)',visual:'dp',accent:dp,code:['prob[start]=1.0;','for(State s:topologicalOrder)','  for(auto [t,p]:transitions(s))','    prob[t]+=prob[s]*p;','// expectation uses E[s]=cost+sum p*E[t] when acyclic'],concepts:['prob[s] 是抵達狀態 s 的機率質量；初始狀態為 1，其他為 0，出邊機率和為 1。','沿無環狀態順序把 prob[s]·p 傳給後繼，全機率公式保證所有互斥前驅貢獻相加。','終態機率總和應為 1，可作數值檢查；有環期望通常需解線性方程而非直接前推。'],states:['probability mass invariant','distribute by transition probabilities','terminal distribution / expectation']},
  {id:'dp-reconstruction',categoryId:'dynamic-programming',subcategory:'經典 DP',category:'DP',title:'DP Reconstruction',zhTitle:'DP 解答重建',description:'保存最佳轉移來源並由終點逆向還原。',complexity:'O(number of states)',visual:'dp',accent:dp,code:['if(candidate<dp[next]){','  dp[next]=candidate; parent[next]=state; choice[next]=move;','}','for(State s=goal;s!=start;s=parent[s]) answer.push_back(choice[s]);','reverse(answer.begin(),answer.end());'],concepts:['每次改善 dp[next] 時同步保存造成此最佳值的 parent 與 choice，兩者必對應同一次更新。','從 goal 沿 parent 嚴格回到較早狀態，收集的選擇順序與建表方向相反。','反轉後得到一條值等於 dp[goal] 的合法最優解；並列最優可任選或保存多父節點。'],states:['store predecessor on relaxation','backtrack from goal','reverse choices to forward solution']},
  {id:'monotone-queue-optimization',categoryId:'dynamic-programming',subcategory:'DP 最佳化',category:'DP OPT',title:'Monotone Queue DP Optimization',zhTitle:'單調佇列最佳化',description:'在線性滑動候選範圍維護最佳轉移值。',complexity:'O(n)',visual:'dp',accent:dp,code:['deque<int> dq;','for(int i=0;i<n;++i){','  while(!dq.empty() && dq.front()<i-K) dq.pop_front();','  dp[i]=base[i]+score(dq.front());','  while(!dq.empty() && score(dq.back())<=score(i)) dq.pop_back(); dq.push_back(i);','}'],concepts:['若 dp[i] 需要窗口 j∈[i−K,i) 的最大 score(j)，deque 保存仍在窗口且未被支配的索引。','過期者從 front 移除；新 score 不小於 back 時，舊候選更差且更早過期，可永久移除。','front 始終給最佳轉移，每索引進出一次，把 O(nK) 降為 O(n)。'],states:['sliding transition candidates','expire front / dominate back','front supplies optimum']},
  {id:'convex-hull-trick',categoryId:'dynamic-programming',subcategory:'DP 最佳化',category:'DP OPT',title:'Convex Hull Trick',zhTitle:'斜率最佳化',description:'把線性轉移表示成直線極值查詢。',complexity:'O(n) or O(n log n)',visual:'dp',accent:dp,code:['// dp[i] = constant(i) + min_j (m[j]*x[i]+b[j])','void addLine(ll m,ll b){ while(lastLineRedundant(m,b)) hull.pop_back(); hull.push_back({m,b}); }','ll query(ll x){ while(hull.size()>1 && value(hull[0],x)>=value(hull[1],x)) hull.pop_front();','  return value(hull.front(),x); }'],concepts:['將每個候選 j 轉為直線 y=m_j x+b_j，對 x_i 查最小值；DP 枚舉轉成動態直線集合查詢。','斜率與查詢 x 皆單調時，以交點順序維護凸包；新線使中間線永不最優時刪除。','查詢指標只向前移，加入與查詢攤銷 O(1)；非單調時用 Li Chao Tree。'],states:['DP candidates become lines','remove geometrically redundant line','query envelope at x_i']},
  {id:'aliens-optimization',categoryId:'dynamic-programming',subcategory:'DP 最佳化',category:'DP OPT',title:'Aliens Trick',zhTitle:'WQS 二分',description:'對選段數加入懲罰並二分拉格朗日乘數。',complexity:'O(T(n) log C)',visual:'dp',accent:dp,code:['pair<ll,int> solve(ll lambda){','  // optimize originalCost + lambda * groups; tie-break by group count','}','while(lo<hi){ ll mid=(lo+hi)/2;','  if(solve(mid).groups<=K) hi=mid; else lo=mid+1;','}','answer=solve(lo).cost-lo*K;'],concepts:['把「恰選 K 組」限制移入目標：cost+λ·groups；λ 增大時最優解使用組數單調不增。','DP 同時回傳懲罰後成本與組數，並以固定方向處理並列，確保二分 predicate 單調。','找到使組數跨越 K 的 λ 後，扣回 λK 還原原目標；適用具離散凸性的分組 DP。'],states:['Lagrangian penalty λ','binary search monotone group count','subtract λK']},
  {id:'slope-trick',categoryId:'dynamic-programming',subcategory:'DP 最佳化',category:'DP OPT',title:'Slope Trick',zhTitle:'斜率技巧',description:'以兩個 Heap 維護分段線性凸函數。',complexity:'O(log n) per operation',visual:'dp',accent:dp,code:['priority_queue<ll> left; priority_queue<ll,vector<ll>,greater<ll>> right;','void addAbs(ll a){','  if(a<left.top()) minValue+=left.top()-a,left.push(a),right.push(left.top()),left.pop();','  else if(a>right.top()) minValue+=a-right.top(),right.push(a),left.push(right.top()),right.pop();','  else left.push(a),right.push(a);','}'],concepts:['凸分段線性函數可由斜率跳點表示；left/right heaps 分別保存最小值區間兩側的轉折。','加入 |x−a| 會在 a 左右各增加一個斜率跳變；若 a 落在最小區間外，移動邊界並增加必要成本。','heap top 給目前 minimizer 區間端點，minValue 保存函數最小值；平移與截斷亦可用 lazy offset 實作。'],states:['convex slope breakpoints','rebalance minimizer interval','minimum value and argmin range']},
]

export const completionDataDpLessons=makeCatalog(specs)
